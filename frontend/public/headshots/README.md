Drop the two founder headshots in here, named exactly:

  carlos-salcedo.jpg
  rj-cooke.jpg

Paths come from src/data/people.js. Until a file exists, the About page
renders a labelled dashed placeholder in its place rather than a broken
image — so adding the file is the only step needed.

Portrait crop, 4:5, ~800x1000 or larger.
